&НаКлиенте
Процедура ПриступимКОтладке(Команда)
	
	Закрыть(Новый Структура("Адрес, НеУдалятьИзХранилища", Адрес, НеУдалятьИзХранилища));
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)

	ТекстСкрипта = СтрШаблон("ВнешниеОбработки.Создать(""CodeConsole83"").ПоместитьЗапросВоВременноеХранилище(Запрос, Новый УникальныйИдентификатор(""%1""))", ЭтаФорма.ВладелецФормы.УникальныйИдентификатор);
	
КонецПроцедуры
